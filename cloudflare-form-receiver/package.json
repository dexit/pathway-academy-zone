{
  "name": "form-receiver",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev":     "wrangler dev",
    "deploy":  "wrangler deploy",
    "db:setup": "wrangler d1 migrations apply form-receiver-db --remote",
    "db:setup:local": "wrangler d1 migrations apply form-receiver-db --local",
    "types":   "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250511.0",
    "typescript": "^5.8.3",
    "wrangler": "^3.114.4"
  }
}
